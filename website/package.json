{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "generate": "tsx scripts/generate-content.ts",
    "generate:workflow": "tsx scripts/generate-workflow-diagram.ts",
    "generate:workflow:force": "tsx scripts/generate-workflow-diagram.ts --force",
    "generate:all": "npm run generate && npm run generate:workflow",
    "dev": "astro dev",
    "build": "npm run generate:all && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "dotenv": "^17.2.3",
    "openai": "^6.17.0",
    "tsx": "^4.21.0"
  }
}
